-- Database initialization script for MGNREGA backend
-- This is optional - Hibernate will auto-create tables if spring.jpa.hibernate.ddl-auto=update

-- Create database if it doesn't exist (run as postgres superuser)
-- CREATE DATABASE mgnrega;

-- The PerformanceRecord entity will create the table automatically
-- This script is provided for reference only

